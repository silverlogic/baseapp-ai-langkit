[pytest]
DJANGO_SETTINGS_MODULE = testproject.settings.test
# -- recommended but optional:
python_files = tests.py test_*.py *_tests.py
# Ignore baseapp_mcp tests when running testproject (they require mcp extra dependencies)
norecursedirs = baseapp_mcp/tests
